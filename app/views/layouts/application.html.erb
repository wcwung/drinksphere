<!DOCTYPE html>
<html>
<head>
  <title>drinksphere.</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  

  <%= render 'layouts/header' %>
    <div class="hero center">
      <h1 class="boom">Evolved Social Drinking</h1>
      <h2 class="badoom">Discover new favorites and revisit old ones. </h2>
      <!-- Sign up / Sign in buttons if user not signed in -->
      <% if !user_signed_in? %>
    
          <%= link_to new_user_registration_path do%>
            <div class="cta-sign">Sign Up</div>
          <% end %>
      
      <% end %>

    </div>
  <div class="container top-buffer">
    <% flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "alert alert-info") %>
    <% end %>
    <%= yield %>
  </div>
</body>
</html>
